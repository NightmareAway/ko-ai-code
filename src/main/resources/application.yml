spring:
  application:
    name: ko-ai-code-backend
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: cn.ko_ai_code.com.koaicode.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

